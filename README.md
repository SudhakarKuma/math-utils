# Math Utils — C++ CI/CD Demo

A simple C++ project demonstrating Continuous Integration (CI) using GitHub Actions

This repository shows how automated pipelines can:

- Build C++ code
- Run unit tests
- Catch regressions automatically
- Prevent broken code from being merged
